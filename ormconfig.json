{
  "type": "postgres",
  "host": "localhost",
  "database": "test",
  "port": 5432,
  "synchronize": true,
  "entities": [
    "src/entity/**/*.ts"
  ],
  "migrations": [
    "src/migration/**/*.ts"
  ],
  "cli": {
    "migrationsDir": "src/migration"
  }
}